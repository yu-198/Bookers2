
<%= render 'users/list', user: @user,new_book: @new_book %>


	<div class="col-xs-9">
		<h2>Bookers</h2>
			<table class="table">
				<thead>
				<tr>
					<th></th>
					<th>Title</th>
					<th>Opinion</th>
				</tr>
				</thead>
				<tbody>
					<% @books.each do |book|%>
				<tr>
			<td>
<%= attachment_image_tag @user, :profile_image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg", size: '60x60' %></th>
					</td>
					<td>
						<%= link_to book.title, book_path(book) %>
					</td>
					<td>
						<%= link_to book.body %>
					</td>
				</tr>
					<% end %>
				</tbody>
			</table>
	</div>
